# 变更影响面速查表

> 目标：当你准备改一个点，但不确定会波及哪些层时，先看这张表。

## 1. 工单 stage / status / priority

| 改动点 | 常影响文件 | 常影响 API | 常影响表 | 验证 |
| --- | --- | --- | --- | --- |
| `work_order_stage` 枚举 | `schema.ts`、`work-order/server.ts`、工单页面 label 映射 | `/api/work-orders`、`/[id]`、`/[id]/stage-assets` | `work_orders`、`missing_items`、`work_order_document_links` | 新建/更新工单、阶段切换、资料上传 |
| `work_order_status` 枚举 | 同上 + 列表页 badge/筛选 | `/api/work-orders/**` | `work_orders` | 列表页、详情页、统计卡片 |
| `work_order_priority` 枚举 | 列表页/看板 label | `/api/work-orders/**` | `work_orders` | 列表、详情、工程看板 |

## 2. 资产上传 / 文档落盘

| 改动点 | 常影响文件 | 常影响 API | 常影响表/存储 | 验证 |
| --- | --- | --- | --- | --- |
| 上传逻辑 | `content/server.ts`、`desktop-file-browser.tsx` | `/api/assets/upload` | `.bpai/storage`、`content_assets`、`file_placements` | 上传、刷新、下载 |
| 文件类型识别 | `content/server.ts`、文档详情页 | `/api/assets/upload`、`/docs/documents/[docId]` | `content_assets` | doc/xlsx/ppt/dwg 各测一遍 |
| OnlyOffice key | `content/server.ts`、OnlyOffice editor、callback route | `/api/onlyoffice/callback` | 资产内容 | 在线编辑回写 |

## 3. 浏览器状态 / 合作空间共享态

| 改动点 | 常影响文件 | 常影响 API | 常影响表 | 验证 |
| --- | --- | --- | --- | --- |
| 个人浏览状态 | `content/browser-state.ts`、`desktop-file-browser.tsx` | `/api/browser-state` | `user_workspace_view_states` | 切视图/刷新 |
| workspace 共享态 | 同上 | `/api/browser-state` | `workspace_shared_states` | 多个空间视图切换 |
| 回收站状态 | `content/server.ts`、trash 页面 | `/api/assets/trash`、`/api/trash/restore`、`/api/trash/clear` | `file_placements` / asset 状态 | 删除、恢复、清空 |

## 4. 合作空间 / 系统表单

| 改动点 | 常影响文件 | 常影响 API | 常影响表/状态 | 验证 |
| --- | --- | --- | --- | --- |
| 创建合作空间 | `workspace/server.ts`、workspace 页面组件 | `/api/collaboration-spaces` | `workspaces`、`collaboration_spaces`、raw shadow | 创建后列表/详情刷新 |
| 成员管理 | `workspace/server.ts` | `/api/workspace-members` | `workspace_members` 或过渡态 store | 成员增删 |
| 系统表单 | `workspace/server.ts`、create-system-form-button | `/api/system-forms` | workspace 相关状态 | 创建后表单可见 |

## 5. 工程队 / 编队 / 派单

| 改动点 | 常影响文件 | 常影响 API | 常影响表 | 验证 |
| --- | --- | --- | --- | --- |
| 编队创建/编辑 | `engineering-team/server.ts`、board 页 | `/api/engineering/squads`、`/squads/[id]` | `engineering_squads`、`engineering_squad_members` | 创建编队 |
| 派单 | `work-order/server.ts`、派单按钮 | `/api/work-orders/[id]/dispatch-executions` | `dispatch_executions`、`engineering_*` | 派单后看板变化 |
| 工程看板汇总 | `engineering/server.ts` | `/engineering/board` 服务端取数 | `engineering_*` + `work_orders` | 看板刷新 |

## 6. BP Ask intent / memory / execution

| 改动点 | 常影响文件 | 常影响 API | 常影响表 | 验证 |
| --- | --- | --- | --- | --- |
| intent / domain / execution mode | `bp-ask/intents.ts`、`bp-ask/dispatch.ts` | `/api/bp-ask/dispatch`、`/threads/[id]/messages` | `execution_tasks` | 预判和正式消息都测 |
| rolling summary | `bp-ask/server.ts` | `/threads/[id]/messages` | `conversation_summaries` | 多轮对话后检查摘要 |
| memory fact | `bp-ask/server.ts` | `/threads/[id]/messages` | `memory_facts` | 同主题多轮对话 |
| 执行结果结构 / 模拟执行预案 | `bp-ask/server.ts`、`bp-ask/shared.ts`、`bp-ask/dispatch.ts`、`bp-ask-workbench.tsx` | `/threads/[id]/messages`、`/dispatch` | `execution_results`、message metadata | 看前端折叠细节与 execution payload |
| 普通聊天生成 / system prompt | `bp-ask/kimi.ts`、`bp-ask/dispatch.ts` | `/dispatch`、`/threads/[id]/messages` | `conversation_messages`、`execution_results` | 问候/身份/能力类对话 |
| 历史线程删除 | `bp-ask-sidebar.tsx`、`bp-ask-shell.tsx`、`bp-ask/server.ts` | `DELETE /api/bp-ask/threads/[id]` | `conversation_threads` | 删除后列表与详情状态 |
| 即时发送体验 | `bp-ask-shell.tsx`、`bp-ask-workbench.tsx` | `/threads/[id]/messages` | 无新增表，影响 thread/message 展示时序 | 用户消息即时上屏、assistant loading |

## 7. 认证 / session

| 改动点 | 常影响文件 | 常影响 API | 常影响表/状态 | 验证 |
| --- | --- | --- | --- | --- |
| 登录逻辑 | `auth/server.ts`、login-form | `/api/auth/login` | `users`、`sessions` | 登录成功/失败 |
| session 恢复 | `auth/server.ts` | `/api/auth/session` | `sessions` | 刷新后仍登录 |
| 登出 | `auth/server.ts` | `/api/auth/logout` | `sessions` | 登出后失效 |

## 8. raw shadow / app-store 过渡层

如果你改的是这些区域，默认把影响面扩大一层：
- `frontend/src/lib/store/raw-shadow.ts`
- `frontend/src/lib/db/app-store-read.ts`
- `frontend/src/lib/db/app-store-write.ts`
- `frontend/src/lib/workspace/server.ts`
- `frontend/src/lib/content/server.ts`
- `frontend/src/lib/auth/server.ts`

原因：
- 这几处常同时影响“正式 Postgres 真源”和“过渡态镜像/旧状态结构”。
- 只改单边，容易导致页面看到的内容和数据库不一致。
